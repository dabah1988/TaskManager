import { ProjectAddRequest } from './project-add-request';

describe('ProjectAddRequest', () => {
  it('should create an instance', () => {
    expect(new ProjectAddRequest()).toBeTruthy();
  });
});
